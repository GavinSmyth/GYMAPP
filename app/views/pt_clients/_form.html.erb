<%= form_with(model: pt_client) do |form| %>
  <% if pt_client.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pt_client.errors.count, "error") %> prohibited this pt_client from being saved:</h2>

      <ul>
        <% pt_client.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :number %>
    <%= form.text_field :number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
